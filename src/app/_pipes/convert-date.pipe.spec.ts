import { ConvertDatePipe } from './convert-date.pipe';

describe('CeonvertDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDatePipe();
    expect(pipe).toBeTruthy();
  });
});
